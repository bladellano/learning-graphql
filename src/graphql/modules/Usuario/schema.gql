type Usuario {
  idade: String
  salario: Float
  nome: String
  email: String
  telefone: String
  ativo: Boolean
  id: ID
  perfil: Perfil
}

type Query {
  usuario(id: Int): Usuario
  usuarios: [Usuario]
}

type Mutation {
  criarUsuario(nome: String, email: String, telefone: String): Usuario
}
